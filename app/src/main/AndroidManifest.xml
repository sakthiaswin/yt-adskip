<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed so MainActivity can prompt the user to exempt this app from
         battery/background restrictions. Without that exemption, some OEM
         builds (Samsung One UI in particular) silently starve the
         accessibility service's window-content IPC calls while the app is
         backgrounded — the service keeps receiving events, but
         rootInActiveWindow()/getWindows() return nothing for as long as
         the restriction is in effect. See YoutubeAdSkipService.kt header
         comment for the field-observed symptom this fixes. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdSkip">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".YoutubeAdSkipService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice.AccessibilityService"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
